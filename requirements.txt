# Core scientific stack
numpy>=1.26,<3.0
pandas>=2.1,<3.0
scipy>=1.11,<2.0
scikit-learn>=1.3,<2.0

# Synthetic data helpers
Faker>=22.0

# Visualisation (validation plots + HTML report)
matplotlib>=3.8

# Optional extras used opportunistically if installed (imports are guarded)
# networkx>=3.2
# plotly>=5.18
